pipeline {
  agent any

  options {
    ansiColor('xterm')
  }

   stages {
       
        stage('Clear') {
           steps {
       
               sh 'npm run report:delete'
              
           }
       }
       
       stage('Dependencies') {
           steps {
       
               sh '''
               npx cypress install --force
               pip install trcli
               '''
              
           }
       }
        stage('e2e Tests Ventes') {
           steps {
               sh '''
               npx cypress run --config video=false,screenshotOnRunFailure=false --reporter junit --reporter-options "mochaFile=reports/TEST-[hash].xml"kkkk'
               junitparser merge --glob "reports/TEST-*" "reports/junit-report_Ventes.xml" 
                '''
  
    }
      post {
    always {
            sh 'trcli -y -h https://facilia.testrail.io/  --project "Facilia" --username  rafael.cruz-exterieur@fiducial.net  --password Suz@no30  parse_junit --title "Cypress API Automated Test Run Ventes" -f "./reports/junit-report_Ventes.xml"'
            
    }
  }      
 }  
}
}
