pipeline {
    agent any
        options {
    ansiColor('xterm')
  }
    stages {
        stage('Test') {
            agent {
                docker {
                    image 'rafaelsuzano/work:latest'
                    args '-u root:root'
                    reuseNode true
                }
            }
            steps {
                 sh 'npm i --unsafe-perm'
                 sh 'npm install --save-dev cypress'
                 sh './Executa_Teste.sh'
            }
        }
    }
}
