{
  "name": "api",
  "version": "1.0.0",
  "description": "teste api",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run",
    "test:login:dev": "cypress run --spec cypress/e2e/web/e2e/login.cy.js --browser chrome --config baseUrl=https://dev.yesaccount.com",
    "test:login:qa": "cypress run --spec cypress/e2e/web/e2e/login.cy.js --browser chrome --config baseUrl=https://recette-qa.facilia.com/"
  },
  "author": "rafael suzano cruz",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.15.0"
  },
  "dependencies": {
    "cypress-mochawesome-reporter": "^3.5.1",
    "cypress-plugin-api": "^2.11.1"
  }
}
