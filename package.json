{
  "name": "api",
  "version": "1.0.0",
  "description": "teste api",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run",
    "test:launchpad": "npx cypress open",
    "test:login": "cypress run --spec cypress/e2e/web/e2e/login.cy.js --browser chrome"
  },
  "author": "rafael suzano cruz",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.15.0"
  },
  "dependencies": {
    "cypress-mochawesome-reporter": "^3.5.1",
    "cypress-plugin-api": "^2.11.1"
  }
}
